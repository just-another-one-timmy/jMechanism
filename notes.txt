notes!
1)в механизме T_TTS_3phase оказалось два одинаковых сегмента s3.  проверить и, если нет, дописать проверку на добавление существующего звена (и др. структурных элементов) с вызовом исключения.
2) надо предусмотреть возможность записи списка груп после анализа, для расставления признаков сборок.
  // CompManager не трогать (не переписывать, т.к. там ничего не могло поменься, но пользователь мог ввести в декартовой системе.) а сохранять именно группы
3) для удобства предлагаю добавить такую фичу: 
   Alt+0176 - символ градуса. 
    если последнийи символ в параметре заданя угла - °, то считать что введен градус.
    если просто число - то это радианы.
    если градус не стоит но число более "6,3" (2 pi)  то вывести Ворнинг : возможна ошибка.